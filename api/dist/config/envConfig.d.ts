declare const envConfig: {
    port: number;
    database: {
        host: string;
        name: string;
        username: string;
        password: string;
        port: number;
    };
};
export default envConfig;
